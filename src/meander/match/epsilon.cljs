(ns ^:no-doc meander.match.epsilon
  (:require [goog.object :as gobj]
            [meander.match.runtime.epsilon :as r.match.runtime]
            [meander.match.specs.epsilon])
  (:require-macros [meander.match.epsilon]))
